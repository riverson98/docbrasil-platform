# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- main

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build images with docker-compose
  jobs:
  - job: Build
    displayName: Build using docker-compose
    pool:
      name: vm-linux
    steps:
    - checkout: self

    - script: |
        echo sudo chmod -R u+rwX /home/azureuser/linuxagent/_work/1/s/nginx/ssl 
      displayName: Da permissao para o usuario root
      RunAsPreJob: true


    - task: AzureKeyVault@2
      inputs:
        azureSubscription: 'DevOps Service Connection'
        KeyVaultName: 'DocBrasilKeyVault'
        SecretsFilter: 'AssociateDbPassword,AuthDbPassword,AssociateAuthKey,AssociateKey,AzureBlobContainerSecretKey,AzureSecretKey'
        RunAsPreJob: true

    - script: |
        cd frontend/doc-brasil
        /usr/bin/npm install
        /usr/bin/npm run build --prod
      displayName: 'Build frontend Angular'

    - script: |
        echo "Remapeando variáveis..."
        echo "MYSQL_ASSOCIADOS_PASSWORD = $MYSQL_ASSOCIADOS_PASSWORD"
        echo "MYSQL_ASSOCIADOS_AUTH_PASSWORD = $MYSQL_ASSOCIADOS_AUTH_PASSWORD"

        export PATH=$PATH:/usr/bin
        /usr/bin/sudo docker compose -f docker-compose.yml -f docker-compose-prod.yml build --no-cache
        /usr/bin/sudo docker compose -f docker-compose.yml -f docker-compose-prod.yml up
      displayName: Build containers with docker-compose
      workingDirectory: $(Build.SourcesDirectory)
      env:
        MYSQL_ASSOCIADOS_PASSWORD: $(AuthDbPassword)
        MYSQL_ASSOCIADOS_AUTH_PASSWORD: $(AssociateDbPassword)
